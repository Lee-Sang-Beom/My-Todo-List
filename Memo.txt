메모장

1) document.getElementById(); 
   ex) document.getElementById.title.innerText = "string" 
       > 화면에 표시된 텍스트 변경

       document.getElementById.style.color = "color"
       > 화면에 표시된 텍스트 색상 변경
   
2) document.querySelect(); 
   - 강추!

3) element.className !== element.classList


4) element.classList.contains();
   element.classList.add();
   element.classList.remove();
   element.claassList.toggle();

5) element.addEventListener("click, mouseenter, mouseleave", function)

6) window.addEventListener("resize, copy, offline, online, ...", function)

7) localstorage.setItem("key", "value")
   localStorage.getItem("key");
   localStorage.removeItem("key");

8) string.padStart("it was required length", if not satisified, should writing string)
                    맞추고자하는 문자열의 길이, 만족되지 않을 시 앞을 뭐로 채울지

9) Math.round(number) : 반올림
   Math.ceil(number) : 무조건 올림
   Math.floor(number) : 무조건 내림
    
10) document.createElement("img"); 이미지라는 태그 생성
    document.body.appendChild(bgImage); body에 appendChild()를 사용해, <img src =""> 추가
    document.body.prepend(bgImage) : 이건 body의 가장앞에 이미지태그를!

11) li.remove(); 해당 li 태그 삭제
    document.createElement("button"); 버튼 엘리먼트 생성
    li.appendChild(span);
    li.appendChild(button); >> <li> <span/> <button/> </li>

12) 자바스크립트 오브젝타나 배열이나 어떤것이든 string으로 만들어주는 기법
   const player = {name : "nico"}
   JSON.stringify(player) = "{\"name\" : \"nico\"}";

   JSON.parse(player)는 반대의 역할을 하여, string을 js가 읽을수있는 살아있는 array로 만듬

13) array.forEach(()=>{})
    array.filter(()=>{return true}) > 포함하고자 하는 object나 요소를 배열에 포함시키고자 하면 반드시 true를 리턴해야함

14) navigator.geolocation.getCurrentPosition("성공 시 실행함수", "실패시 실행함수)
    위치좌표줌.

15) fetch(url); > url 데이터를 불러옴